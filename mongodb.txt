to clear lichess database
show dbs
use x
db.dropDatabase()

to install
brew install redis
brew services start redis

brew install nvm
nvm install 24
nvm use 24

brew tap mongodb/brew
brew update
brew install mongodb-community
brew services start mongodb-community

brew install java
curl -fL https://github.com/coursier/coursier/releases/latest/download/cs-aarch64-apple-darwin.gz | gzip -d > cs
sudo chmod +x cs
./cs setup
sudo rm -rf cs

to install lila
git clone --recursive https://github.com/lichess-org/lila.git
cd lila
corepack enable
mongosh lichess < bin/mongodb/indexes.js
ui/build
./lila.sh

edit lila/modules/web/src/main/helper/AssetFullHelper.scala
if netConfig.siteName == "liladocker.duckdns.org:9663" then "lichess.dev"

edit lila/conf/base.conf
domain = "liladocker.duckdns.org:9663"
ratelimit = false

git clone https://github.com/lichess-org/lila-ws.git
cd lila-ws
sbt run -Dcsrf.origin=http://liladocker.duckdns.org:9663

to install homebrew
NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/uninstall.sh)"
sudo rm -rf /opt/homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

to install trainingdata-tool
edit trainingdata-tool/zlib/gzlib.c, trainingdata-tool/zlib/gzread.c, trainingdata-tool/zlib/gzwrite.c
#include "unistd.h"

edit trainingdata-tool/CMakeLists.txt
if (APPLE)
    target_link_libraries(trainingdata-tool -lpthread)
elseif (UNIX)